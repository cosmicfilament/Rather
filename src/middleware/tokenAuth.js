export const tokenAuth = (store) => (next) => (action) => {
    //set a timer to fire a dispatch every minute to check the token
    // in the middleware we will auto renew the token for 5 minutes while use is logged in.then(()=>{

})
};
